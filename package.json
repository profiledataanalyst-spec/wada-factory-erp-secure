{
  "name": "profile-solutions-factory-erp",
  "version": "11.0.0",
  "private": true,
  "description": "Profile Solutions Factory ERP stability release with atomic idempotent Supabase mutations, serialized authentication startup, incremental Realtime synchronization, controlled retries and production diagnostics",
  "scripts": {
    "check": "node scripts/check.mjs",
    "test:stability": "node scripts/stability-tests.mjs",
    "audit": "node scripts/audit.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
